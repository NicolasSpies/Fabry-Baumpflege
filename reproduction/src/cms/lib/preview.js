/**
 * Metadata helper for ContentBridge scanning.
 * This file helps expose static preview data from the frontend without executing the full runtime.
 */

/**
 * Define preview metadata for a page or component.
 * @param {Object} data - The structured preview data.
 */
export function definePreview(data) {
    return data;
}
